1  GOTO 100: REM PROGRAMA PRINCIPAL
2  REM *** ROTINAS UTILITARIAS MC-1000
3  REM ** LOCATE H,V : POSICIONA CURSOR
4  POKE 910,0: PRINT AT$; CHR$ (V); CHR$ (H + 32);: POKE 910,H AND 31: RETURN 
100  REM *** PROGRAMA PRINCIPAL
110  GOSUB 60000: REM INICIALIZA MC-1000
120  REM LACO PRINCIPAL
130 Y = 5: REM POSICAO VERTICAL DO CORREDOR
140  FOR X = 0 TO 26: REM POSICAO HORIZONTAL DO CORREDOR
150  GOSUB 1000: REM DESENHA CORREDOR (QUADRO 1)
160  GOSUB 3000: REM PAUSA
170  GOSUB 2000: REM DESENHA CORREDOR (QUADRO 2)
180  GOSUB 3000: REM PAUSA
190  NEXT X
200  END 
1000  REM ** DESENHA CORREDOR (QUADRO 1)
1010 H = X:V = Y: GOSUB 4: PRINT  CHR$ (128); CHR$ (128); CHR$ (128); CHR$ (190); CHR$ (128); CHR$ (128);
1020 H = X:V = Y + 1: GOSUB 4: PRINT  CHR$ (128); CHR$ (134); CHR$ (176); CHR$ (191); CHR$ (131); CHR$ (128);
1030 H = X:V = Y + 2: GOSUB 4: PRINT  CHR$ (128); CHR$ (128); CHR$ (132); CHR$ (174); CHR$ (141); CHR$ (128);
1040 H = X:V = Y + 3: GOSUB 4: PRINT  CHR$ (128); CHR$ (128); CHR$ (129); CHR$ (168); CHR$ (149); CHR$ (130);
1050 H = X:V = Y + 4: GOSUB 4: PRINT  CHR$ (128); CHR$ (132); CHR$ (162); CHR$ (128); CHR$ (128); CHR$ (128);
1060  RETURN 
2000  REM ** DESENHA CORREDOR (QUADRO 2)
2010 H = X:V = Y: GOSUB 4: PRINT  CHR$ (128); CHR$ (128); CHR$ (128); CHR$ (143); CHR$ (128); CHR$ (128);
2020 H = X:V = Y + 1: GOSUB 4: PRINT  CHR$ (128); CHR$ (133); CHR$ (141); CHR$ (174); CHR$ (156); CHR$ (128);
2030 H = X:V = Y + 2: GOSUB 4: PRINT  CHR$ (128); CHR$ (144); CHR$ (157); CHR$ (162); CHR$ (128); CHR$ (128);
2040 H = X:V = Y + 3: GOSUB 4: PRINT  CHR$ (128); CHR$ (131); CHR$ (151); CHR$ (144); CHR$ (138); CHR$ (128);
2050 H = X:V = Y + 4: GOSUB 4: PRINT  CHR$ (128); CHR$ (160); CHR$ (128); CHR$ (128); CHR$ (171); CHR$ (128);
2060  RETURN 
3000  REM ** PAUSA
3010  FOR A = 1 TO 20: NEXT : RETURN 
60000  REM *** INICIALIZA MC-1000
60010  REM CONSTANTES
60020 AT$ =  CHR$ (27) + "=": REM ESCAPE P/ POSICIONAR CURSOR
60030  REM CONFIGURA VIDEO
60040  TEXT : HOME 
60050  POKE 245,97: REM MODO SEMIGRAPHICS 6,PALETA 0
60060  POKE 247,202: POKE 248,192: POKE 249,202: POKE 250,192: CALL 52013: REM CURSOR INVISIVEL
60070  CALL 54492: REM LIMPA VRAM COM BYTES 0
60080  RETURN 
65500  REM *** CATALOGACAO
65501  REM REVISTA INPUT (BR)
65502  REM VOL.1,NO.1,PAG.6
65503  REM PROGRAMACAO DE JOGOS 1
65504  REM "ANIMACAO E SINAIS GRAFICOS"
65505  REM (CORREDOR)
65506  REM VERSAO CCE MC-1000
65507  REM POR EMERSON COSTA
65508  REM EM 2026-02-06
